```Algoritmo Distancia_cero
	Escribir "Escribe un numero"
	leer numero
	Para i=1 Hasta 4 Con Paso 1 Hacer
		Escribir "Escribe un numero"
		leer numeros
		SI Abs(numeros) > Abs(numero) Entonces
			numero = numeros
		FinSi
	FinPara
	escribir Trunc(numero)
FinAlgoritmo```
